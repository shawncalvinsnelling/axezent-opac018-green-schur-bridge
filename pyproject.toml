[project]
name = "axezent-opac018-green-schur-bridge"
version = "2.1.0"
description = "OPAC-018 Green-Schur Bridge external-review package with exact audits, examples, tests, and receipts."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Shawn Calvin Snelling"}]

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.axezent]
truth_label = "SOLUTION-CANDIDATE / EXTERNAL REVIEW PENDING"
audit_scripts = ["pure_python_exact_audit.py", "counterexample_stress_test.py", "verify_all.py", "verify_manifest.py"]
